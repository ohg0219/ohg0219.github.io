---
name: blog-draft
description: 현재 Claude Code 대화 컨텍스트와 `.claude/blog-session.md` 의 메타데이터를 분석해 Jekyll 포스트 마크다운 초안을 `_posts/YYYY-MM-DD-<slug>.md` 로 생성한다. 사용자는 이후 로컬 에디터에서 검토·수정 후 git push 한다.
---

# blog-draft

이 Skill 이 호출되면 다음 절차를 따른다.

## 1. 메타데이터 로드

- `.claude/blog-session.md` 를 읽는다.
- **없으면** 사용자에게 안내하고 종료:
  > "블로깅 세션이 시작되지 않았습니다. 먼저 `/blog-start <주제>` 를 실행하시거나, 지금 주제를 알려주시면 시작 단계 없이 바로 초안을 작성할게요."
  > (사용자가 즉시 주제를 알려주면 그것을 받아 진행하되, `.claude/blog-session.md` 도 함께 생성)
- frontmatter 에서 `topic`, `slug`, `categories`, `tags` 추출.

## 2. 대화 분석

현재 Claude Code 대화 전체를 다음 4섹션으로 정리한다.

### 문제 상황 / 발단
- 사용자가 처음 무엇을 하려 했는가
- 어디서 막혔는가 / 어떤 에러·증상이 있었는가
- 환경(OS, 언어/프레임워크 버전 등) 이 대화에 나왔으면 명시

### 시도와 발견
- 거친 시행착오 — 의미 있는 것 2~4개 (모든 삽질을 다 적지 말 것)
- 핵심 단서가 된 정보(로그 한 줄, 문서 한 문장, Stack Overflow 답변 등)
- 가설→검증 흐름이 있으면 그 구조를 살린다

### 해결책
- 최종적으로 동작한 코드/명령/설정
- 코드 블록은 반드시 언어 태그 포함 (` ```java `, ` ```yaml `, ` ```powershell `)
- 한 가지 방법으로 좁혀서 명확하게. 대안이 있으면 "다른 방법: ..." 으로 짧게만 언급

### 배운 점 / 다음 번엔
- 일반화할 수 있는 교훈 (특정 라이브러리·도구 한정 팁이 아니라 사고 방식)
- 다음에 비슷한 상황을 만났을 때 가장 먼저 확인해야 할 한두 가지

## 3. 포스트 파일 생성

`_posts/<YYYY-MM-DD>-<slug>.md` 를 Write 툴로 새로 만든다(이미 존재하면 슬러그 뒤에 `-2`, `-3` 을 붙여 충돌 회피).

**Frontmatter**:
```yaml
---
layout: post
title: "<topic>"
date: <YYYY-MM-DD HH:MM:SS +0900>
categories: [<categories>]
tags: [<tags>]
---
```

**본문 구조** (h2 헤딩 사용):
```markdown
## 문제 상황

...

## 시도와 발견

...

## 해결책

...

## 배운 점

...
```

## 4. 작성 원칙

- **톤**: 대화에서 사용자의 톤(반말/존댓말)을 감지해 본문 톤을 맞춘다. 기본은 존댓말("~합니다").
- **민감 정보 마스킹**: API 키, 비밀번호, 사내 서버 주소·IP, 개인 이메일, 인증 토큰이 발견되면 `sk-xxxx`, `<INTERNAL_HOST>`, `xxx@example.com` 등으로 치환하고 본문에 ⚠️ 한 줄 주석을 남긴다.
- **출처**: 대화 중 등장한 외부 링크(Stack Overflow, 공식 문서, GitHub Issue)는 본문 끝 "참고" 섹션에 모은다.
- **코드 보존**: 최종 동작한 버전을 우선. 시행착오의 의미 있는 중간 단계는 1~2개만 보존.
- **분량**: 800~1500 단어 권장. 너무 길면 frontmatter 직전 HTML 주석으로 `<!-- TODO: 후속편 분리 권장 -->` 남기되 본문은 그대로 작성.
- **반말/존댓말 자동 변환은 본문만**. 코드·로그·에러 메시지는 원문 그대로.

## 5. 사용자 안내

작성 후 다음을 한 번에 출력:

```
📝 초안 작성 완료
   파일: _posts/<YYYY-MM-DD>-<slug>.md

다음 단계:
  1) 에디터에서 열어 검토·수정
  2) 발행:
     git add _posts/<YYYY-MM-DD>-<slug>.md
     git commit -m "post: <topic>"
     git push

   배포 후 약 1분 뒤:
   https://ohg0219.github.io/<YYYY>/<MM>/<DD>/<slug>/
```

## 6. 부수 동작

- 초안 작성에 성공했으면 `.claude/blog-session.md` 의 frontmatter 에 `drafted_at: <YYYY-MM-DD HH:MM>` 을 추가(같은 세션에서 재실행 시 추적용).
- 같은 세션에서 두 번째 호출이면 사용자에게 "이미 초안이 있어요(`<기존 파일>`). 1) 덮어쓰기 2) 새 파일로 분리(`-2` 접미사) 중 어느 쪽?" 을 묻는다.
